// ============= GENERATOR CONSTANTS =============

// Retry configuration
export const MAX_RETRY_ATTEMPTS = 5;
export const RETRY_DELAYS = [2000, 4000, 8000, 15000, 30000];

// Retry configuration for scene types
export const MAX_SCENE_RETRY_ATTEMPTS = 3;
export const SCENE_RETRY_DELAYS = [1000, 2000, 4000];
